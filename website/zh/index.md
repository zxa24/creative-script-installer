---
title: Chinese locale (stub)
description: Placeholder for the Chinese locale. No content has been written yet.
---

# Chinese locale — not written yet

This locale exists so the i18n structure is already in place. No pages have
been translated.

Use the [English documentation](/) for now.

<!--
  TRANSLATOR NOTES

  The i18n wiring lives in `.vitepress/config.mjs` under `locales.zh`.
  To fill this locale in:

  1. Mirror the English page tree under `website/zh/`
     (`zh/index.md`, `zh/guide/…`, `zh/scripts/…`, `zh/reference/…`).
  2. Give `locales.zh` its own `themeConfig.nav` and `themeConfig.sidebar`.
  3. Decide whether the llms.txt surface should cover this locale. Today
     `.vitepress/gen-llms.mjs` deliberately skips `zh/` — see LOCALE_EXCLUDE
     in that file.
-->
