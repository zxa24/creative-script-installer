import DefaultTheme from "vitepress/theme";
import "./custom.css";

// Thin override of the stock VitePress theme — palette + typography only.
// The theme itself is not forked.
export default DefaultTheme;
